const tambah = (angka_1, angka_2) => {
  const hasil = angka_1 + angka_2;
  console.log(hasil);
};

const kurang = (angka_1, angka_2) => {
  const hasil = angka_1 - angka_2;
  console.log(hasil);
};

const kali = (angka_1, angka_2) => {
  const hasil = angka_1 * angka_2;
  console.log(hasil);
};

const bagi = (angka_1, angka_2) => {
  const hasil = angka_1 / angka_2;
  console.log(hasil);
};

tambah(1, 2);
kurang(3, 2);
kali(2, 2);
bagi(6, 2);
