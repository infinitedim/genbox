const kali = (a: number, b: number): number => a * b;
const bagi = (a: number, b: number): number => a / b;
const kurang = (a: number, b: number): number => a - b;
const tambah = (a: any, b: any): number => a + b;

export { kali, bagi, kurang, tambah };
